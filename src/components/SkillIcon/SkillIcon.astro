---
interface Props {
  iconUrl: string[] | undefined;
  caption?: string;
  size?: number;
}

const { iconUrl ,caption, size = 12 } = Astro.props;
---

<figure class="flex flex-col items-center">
  <img
    src=`https://skillicons.dev/icons?i=${iconUrl}`
    alt=`${caption} icon`
    class=`w-${size} h-${size}`
  />
  <figcaption class="mt-2 text-sm text-black">
    {caption}
  </figcaption>
</figure>
